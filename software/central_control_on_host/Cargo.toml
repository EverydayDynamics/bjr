[package]
name = "central_control_on_host"
version = "0.1.0"
edition = "2021"

[dependencies]
board_support_package = { path = "../board_support_package", version = "0.1.0", features = ["mock_board"]}
os_traits = {path = "../os_traits"}
application_builder = { path = "../application_builder", version = "0.1.0"}
application_logic = {path = "../application_logic", version = "0.1.0"}
strum = { version = "0.26.3", default-features = false, features = ["derive"] }
strum_macros = "0.26.4"
embedded-time = "0.12.1"